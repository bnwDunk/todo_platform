name: Deploy to VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd /home/devops/todo_platform
          git pull origin main
          docker compose up -d --build
          docker image prune -f